services:
  - type: web
    name: texas-divorce-database
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python download_db.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 1 --timeout 120
    healthCheckPath: /
